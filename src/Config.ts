import { IDriveInfo } from "@fluid-dev/m365-utils";

export const pushServiceToken =
    "";
export const sharePointToken =
    "";

export const driveInfo: IDriveInfo = {
    siteUrl:
      "",
    driveId:
      "",
};